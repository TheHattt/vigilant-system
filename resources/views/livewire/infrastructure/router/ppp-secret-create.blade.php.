<form wire:submit="save" class="space-y-6">
    <div class="space-y-4">
        <flux:input
            label="Username"
            wire:model="name"
            placeholder="e.g. customer_123"
            icon="user"
        />

        <flux:input
            label="Password"
            type="password"
            wire:model="password"
            icon="key"
            viewable
        />

        <div class="grid grid-cols-2 gap-4">
            <flux:select label="Service" wire:model="service">
                <option value="any">Any</option>
                <option value="pppoe">PPPoE</option>
                <option value="pptp">PPTP</option>
                <option value="l2tp">L2TP</option>
                <option value="sstp">SSTP</option>
                <option value="ovpn">OpenVPN</option>
            </flux:select>

            <flux:input
                label="Profile"
                wire:model="profile"
                placeholder="default"
            />
        </div>

        <flux:textarea
            label="Comment"
            wire:model="comment"
            placeholder="Customer details, location, etc."
            rows="2"
        />
    </div>

    <div class="flex gap-2 justify-end">
        <flux:modal.close>
            <flux:button variant="ghost">Cancel</flux:button>
        </flux:modal.close>

        <flux:button type="submit" variant="filled" class="!bg-white !text-black">
            Create User
        </flux:button>
    </div>
</form>
